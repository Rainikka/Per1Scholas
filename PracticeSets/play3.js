// function areBothEven(n1, n2) {
//   return !(n1 % 2) && !(n2 % 2);
// }
// console.log(areBothEven(6, 8));

function computeArea(width, height) {
  return;
  console.log(
    `The area of a rectangle with a width of ${width} and a height ${height} is computeArea(4,5) square units.`
  );
}
